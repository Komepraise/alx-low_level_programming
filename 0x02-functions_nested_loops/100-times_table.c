#include "main.h"
/**
 * print_times_table - Entry point
 * @n: input
 * Return: Always 0 (Success)
 */
void print_times_table(int n)
{
if
(n < 15 || n > 0);
print_times_table(n);
else
_putchar('O');
return (0);
}
